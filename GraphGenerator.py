print("\n# Graph Generator #")

height = int(input("\nInput Rows:"))
width = int(input("Input Columns:"))

rows = []
columns = []

for x in range(height + 1):
    rows.append(x)

for y in range(columns + 1):
    columns.append(y)
    
